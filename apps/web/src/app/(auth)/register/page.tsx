import { RegisterForm } from "./_components/register-form";

export const metadata = {
  title: "Create Account — WhatsApp AI Platform",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
